#include <bits/stdc++.h>

using namespace std;

int main() {

  int a,b,c;
  int k = 0;
  if (n < 3){
    cout << -1;
  }else{
    cin >> a >> b >> c;
    if(a==b && b==c) k++;
      n -= 3
  }

  return 0;
}
